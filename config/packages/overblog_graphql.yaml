overblog_graphql:
    definitions:
        schema:
            default:
                query: Query
                mutation: Mutation
        mappings:
            auto_discover: false
            types:
                - dir: '%kernel.project_dir%/config/graphql/types'
                  types: [yaml]
                  suffix: ~
